# Lambda Labs GPU-Optimized Requirements
# Ultra-fast Northeastern University Chatbot

# Web Framework (REQUIRED)
fastapi==0.108.0
uvicorn[standard]==0.25.0

# OpenAI and LangChain (REQUIRED) - Optimized versions
openai>=1.6.1,<2.0.0
langchain==0.1.0
langchain-openai==0.0.2
langchain-community==0.0.13

# Vector Database (REQUIRED)
chromadb==1.0.15

# GPU-Accelerated NLP and Embeddings (REQUIRED)
sentence-transformers==2.2.2
transformers==4.36.0
torch>=2.0.0
torch-audio>=2.0.0
torch-vision>=2.0.0
numpy<2.0.0

# GPU Optimization (REQUIRED for Lambda Labs)
accelerate>=0.24.0
bitsandbytes>=0.41.0
flash-attn>=2.3.0
xformers>=0.0.20

# Utilities (REQUIRED)
python-dotenv==1.0.0
pydantic==2.5.0
python-multipart==0.0.6
requests==2.31.0
aiofiles==23.2.1

# Performance Monitoring (REQUIRED)
psutil>=5.9.0
nvidia-ml-py3>=7.352.0
gpustat>=1.1.1

# Caching and Optimization (REQUIRED)
redis>=5.0.1
diskcache>=5.6.3

# Async Processing (REQUIRED)
asyncio-mqtt>=0.16.1
aiohttp>=3.9.0

# Development and Testing (OPTIONAL)
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Optional: Advanced GPU Features
# triton>=2.1.0  # For advanced GPU kernels
# apex>=0.1  # For mixed precision training