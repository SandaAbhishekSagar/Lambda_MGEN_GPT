# Lambda Labs GPU-Optimized Requirements
# Core dependencies for Northeastern University Chatbot on Lambda GPU

# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# OpenAI integration
openai>=1.6.1,<2.0.0
langchain-openai==0.0.2
langchain==0.0.340
langchain-community==0.0.10

# GPU-accelerated machine learning
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
torchaudio>=2.0.0,<3.0.0
transformers>=4.35.0,<5.0.0
sentence-transformers>=2.2.2,<3.0.0
accelerate>=0.24.0,<1.0.0

# Vector database
chromadb>=0.4.15,<1.0.0

# Data processing
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0

# HTTP requests and utilities
requests>=2.31.0,<3.0.0
httpx>=0.25.0,<1.0.0

# Environment and configuration
python-dotenv>=1.0.0,<2.0.0
python-multipart>=0.0.6,<1.0.0

# Caching and optimization
redis>=5.0.0,<6.0.0
diskcache>=5.6.0,<6.0.0

# GPU monitoring and optimization
nvidia-ml-py3>=7.352.0,<8.0.0
psutil>=5.9.0,<6.0.0

# Logging and monitoring
structlog>=23.2.0,<24.0.0

# Security
cryptography>=41.0.0,<42.0.0

# Development and testing (optional)
pytest>=7.4.0,<8.0.0
pytest-asyncio>=0.21.0,<1.0.0

# Lambda Labs specific optimizations
# These packages are optimized for Lambda's GPU infrastructure
einops>=0.7.0,<1.0.0  # Efficient tensor operations
flash-attn>=2.3.0,<3.0.0  # Flash attention for faster inference
xformers>=0.0.20,<1.0.0  # Memory-efficient attention

# Optional: For advanced GPU optimization
# triton>=2.1.0,<3.0.0  # GPU kernel compilation
# apex>=0.1  # NVIDIA Apex for mixed precision training

# Note: Some packages may need to be installed from source on Lambda Labs
# due to CUDA version compatibility. Check Lambda Labs documentation for
# specific CUDA versions and compatible package versions.
